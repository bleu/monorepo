{
  "name": "pool-matadata-registry-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pool-matadata-registry-graph",
    "create-local": "graph create --node http://localhost:8020/ pool-matadata-registry-graph",
    "remove-local": "graph remove --node http://localhost:8020/ pool-matadata-registry-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pool-matadata-registry-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.3",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
